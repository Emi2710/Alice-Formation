import React from 'react'

const HeroBanner = ({ heroBanner }) => {
  return (
    <div>
        <div>
            <h1>{heroBanner.title}</h1>
        </div>
    </div>
  )
}

export default HeroBanner